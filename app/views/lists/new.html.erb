<div class="container">
  <div class="row justify-content-center">
    <div class="col-12 col-lg-6">
      <%= simple_form_for(@list) do |f| %>
        <%= f.input :name %>
        <%= f.submit %>
      <% end %>

      <%= link_to 'Back to list', lists_path(@lists), class: "btn btn-primary mt-3" %>
    </div>
  </div>
</div>
